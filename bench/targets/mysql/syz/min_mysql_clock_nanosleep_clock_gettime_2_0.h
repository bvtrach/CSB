/*
 * Copyright (C) Huawei Technologies Co., Ltd. 2026. All rights reserved.
 * SPDX-License-Identifier: MIT
 */
// autogenerated by syzkaller (https://github.com/google/syzkaller)
// clang-format off
#ifndef CSB_PROGRAM_NAME
#define CSB_PROGRAM_NAME ""
#endif
#define MMAP_OFFSET 0x20000000ul
#define MMAP_LENGTH 0x1000000ul
const static uint64_t maxWriteBufferSize = 0ul;
const static uint64_t maxWriteBufferSizeAlignment = 4096ul;
static const char* netops_connect[0] __attribute__((unused)) = {};
static const char* netops_accept[0] __attribute__((unused)) = {};

#include <endian.h>
#include <errno.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/types.h>
#include <unistd.h>

#ifndef __NR_clock_gettime
#define __NR_clock_gettime 113
#endif
#ifndef __NR_clock_nanosleep
#define __NR_clock_nanosleep 115
#endif
#ifndef __NR_mmap
#define __NR_mmap 222
#endif

#include <fcntl.h> /* Definition of AT_* constants */
#ifndef BM_THREAD_NUM
#define BM_THREAD_NUM 1
#endif
#ifndef BM_THREAD_IDX
#define BM_THREAD_IDX 0
#endif
#ifndef BM_CTX_TID
#define BM_CTX_TID 0
#endif
#define MMAP_LEN (0x1000 + 0x1000000 + 0x1000)
#define MMAP_SIZE_TOTAL ((BM_THREAD_NUM) * (MMAP_LEN))

const static int num_subdirs = 0;
const static char* subdirs[0] = {};
const static int num_filenames = 0;
const static char* filenames[0] = {};
const static int num_filesizes = 0;
const static uint64_t filesizes[0] = {};

static void __attribute__((noinline)) remove_tmp_dir(const char* dir)
{
	DIR* dp = opendir(dir);
	if (dp == NULL) {
		if (errno == EACCES) {
			if (rmdir(dir))
	assert(0);
			return;
		}
	assert(0);
	}
	struct dirent* ep = 0;
	while ((ep = readdir(dp))) {
		if (strcmp(ep->d_name, ".") == 0 || strcmp(ep->d_name, "..") == 0)
			continue;
		char filename[FILENAME_MAX];
		snprintf(filename, sizeof(filename), "%s/%s", dir, ep->d_name);
		struct stat st;
		if (lstat(filename, &st))
	assert(0);
		if (S_ISDIR(st.st_mode)) {
			remove_tmp_dir(filename);
			continue;
		}
		if (unlink(filename)) {
	assert(0);
		}
	}
	closedir(dp);
	while (rmdir(dir)) {
	assert(0);
	}
}

static inline int
bm_target_reg(thread_ctx_t* ctx)
{
		intptr_t res = 0;
	V_UNUSED(res);
	ctx->r = (uint64_t*)malloc(sizeof(uint64_t)*1);
	ctx->r[0] = 0x0;
	return 0;
}
static inline int
bm_target_dereg(thread_ctx_t* ctx)
{
		free(ctx->r);
	return 0;
}

static inline int bm_dispatch_operation(thread_ctx_t* ctx, size_t op_id)
{
	const char* reason;
	(void)reason;
			
	intptr_t res = 0;
	V_UNUSED(res);
//  clock_gettime arguments: [
//    id: clock_id = 0x3 (8 bytes)
//    tp: ptr[out, timespec] {
//      timespec {
//        sec: time_sec (resource)
//        nsec: time_nsec (resource)
//      }
//    }
//  ]
	res = syscall(__NR_clock_gettime, /*id=CLOCK_THREAD_CPUTIME_ID*/3ul, /*tp=*/0x20157d40ul+PTR_OFFSET);
	if (res == -1 ) { assert(!abort_on_fail); ctx->num_failed++;} else {ctx->num_succeeded++;};
	if (res != -1)
ctx->r[0] = *(uint64_t*)(0x20157d40ul+PTR_OFFSET);
//  clock_nanosleep arguments: [
//    id: clock_id = 0x0 (8 bytes)
//    flags: timer_flags = 0x0 (8 bytes)
//    rqtp: ptr[in, timespec] {
//      timespec {
//        sec: time_sec (resource)
//        nsec: time_nsec (resource)
//      }
//    }
//    rmtp: ptr[out, timespec] {
//      timespec {
//        sec: time_sec (resource)
//        nsec: time_nsec (resource)
//      }
//    }
//  ]
*(uint64_t*)(0x20119300ul+PTR_OFFSET) = ctx->r[0];
*(uint64_t*)(0x20119308ul+PTR_OFFSET) = 0x1e8480;
	res = syscall(__NR_clock_nanosleep, /*id=*/0ul, /*flags=*/0ul, /*rqtp=*/0x20119300ul+PTR_OFFSET, /*rmtp=*/0x20119340ul+PTR_OFFSET);
	if (res == -1 ) { assert(!abort_on_fail); ctx->num_failed++;} else {ctx->num_succeeded++;};
	return 0;
}
