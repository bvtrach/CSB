/*
 * Copyright (C) Huawei Technologies Co., Ltd. 2026. All rights reserved.
 * SPDX-License-Identifier: MIT
 */
// autogenerated by syzkaller (https://github.com/google/syzkaller)
// clang-format off
#ifndef CSB_PROGRAM_NAME
#define CSB_PROGRAM_NAME ""
#endif
#define MMAP_OFFSET 0x20000000ul
#define MMAP_LENGTH 0x1000000ul
const static uint64_t maxWriteBufferSize = 4096ul;
const static uint64_t maxWriteBufferSizeAlignment = 4096ul;
static const char* netops_connect[0] __attribute__((unused)) = {};
static const char* netops_accept[0] __attribute__((unused)) = {};

#include <endian.h>
#include <errno.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/types.h>
#include <unistd.h>

#ifndef __NR_close
#define __NR_close 57
#endif
#ifndef __NR_fsync
#define __NR_fsync 82
#endif
#ifndef __NR_mmap
#define __NR_mmap 222
#endif
#ifndef __NR_openat
#define __NR_openat 56
#endif
#ifndef __NR_write
#define __NR_write 64
#endif

#include <fcntl.h> /* Definition of AT_* constants */
#ifndef BM_THREAD_NUM
#define BM_THREAD_NUM 1
#endif
#ifndef BM_THREAD_IDX
#define BM_THREAD_IDX 0
#endif
#ifndef BM_CTX_TID
#define BM_CTX_TID 0
#endif
#define MMAP_LEN (0x1000 + 0x1000000 + 0x1000)
#define MMAP_SIZE_TOTAL ((BM_THREAD_NUM) * (MMAP_LEN))

const static int num_subdirs = 1;
const static char* subdirs[1] = {"var/lib/mysql"};
const static int num_filenames = 1;
const static char* filenames[1] = {"./var/lib/mysql/mysqlx.sock.lock"};
const static int num_filesizes = 1;
const static uint64_t filesizes[1] = {0};

static void __attribute__((noinline)) remove_tmp_dir(const char* dir)
{
	DIR* dp = opendir(dir);
	if (dp == NULL) {
		if (errno == EACCES) {
			if (rmdir(dir))
	assert(0);
			return;
		}
	assert(0);
	}
	struct dirent* ep = 0;
	while ((ep = readdir(dp))) {
		if (strcmp(ep->d_name, ".") == 0 || strcmp(ep->d_name, "..") == 0)
			continue;
		char filename[FILENAME_MAX];
		snprintf(filename, sizeof(filename), "%s/%s", dir, ep->d_name);
		struct stat st;
		if (lstat(filename, &st))
	assert(0);
		if (S_ISDIR(st.st_mode)) {
			remove_tmp_dir(filename);
			continue;
		}
		if (unlink(filename)) {
	assert(0);
		}
	}
	closedir(dp);
	while (rmdir(dir)) {
	assert(0);
	}
}

static inline int
bm_target_reg(thread_ctx_t* ctx)
{
		intptr_t res = 0;
	V_UNUSED(res);
	ctx->r = (uint64_t*)malloc(sizeof(uint64_t)*1);
	ctx->r[0] = 0xffffffffffffffff;
	return 0;
}
static inline int
bm_target_dereg(thread_ctx_t* ctx)
{
		free(ctx->r);
	return 0;
}

static inline int bm_dispatch_operation(thread_ctx_t* ctx, size_t op_id)
{
	const char* reason;
	(void)reason;
			
	intptr_t res = 0;
	V_UNUSED(res);
//  openat arguments: [
//    fd: fd_dir (resource)
//    file: ptr[in, buffer] {
//      buffer: {2e 2f 76 61 72 2f 6c 69 62 2f 6d 79 73 71 6c 2f 6d 79 73 71 6c 78 2e 73 6f 63 6b 2e 6c 6f 63 6b 00} (length 0x21)
//    }
//    flags: open_flags = 0x42 (4 bytes)
//    mode: open_mode = 0x1ff (2 bytes)
//  ]
//  returns fd
memcpy((void*)(0x20100400ul+PTR_OFFSET), "./var/lib/mysql/mysqlx.sock.lock\000", 33);
	res = syscall(__NR_openat, ctx->dirfd, /*file=*/0x20100400ul+PTR_OFFSET, /*flags=O_NONBLOCK|O_CREAT|O_RDWR*/0x842, /*mode=S_IXOTH|S_IWOTH|S_IROTH|S_IXGRP|S_IWGRP|S_IRGRP|S_IXUSR|S_IWUSR|0x100*/0x1ff);
	if (res == -1 ) { assert(!abort_on_fail); ctx->num_failed++;} else {ctx->num_succeeded++;};
	if (res != -1) {
		{ int flags = fcntl(res, F_GETFL); if (flags != -1) fcntl(res, F_SETFL, flags | O_NONBLOCK); }
		ctx->r[0] = res;
	}
//  write arguments: [
//    fd: fd (resource)
//    buf: ptr[in, buffer] {
//      buffer: {} (length 0x0)
//    }
//    count: len = 0x7 (8 bytes)
//  ]
	res = syscall(__NR_write, /*fd=*/ctx->r[0], ctx->writeBuffer, /*count=*/7ul);
	if (res == -1 ) { assert(!abort_on_fail); ctx->num_failed++;} else {ctx->num_succeeded++;};
//  fsync arguments: [
//    fd: fd (resource)
//  ]
	res = syscall(__NR_fsync, /*fd=*/ctx->r[0]);
	if (res == -1 ) { assert(!abort_on_fail); ctx->num_failed++;} else {ctx->num_succeeded++;};
//  close arguments: [
//    fd: fd (resource)
//  ]
	res = syscall(__NR_close, ({ intptr_t csb_fd = (/*fd=*/ctx->r[0]); (uint32_t)csb_fd <= 2 ? -1 : csb_fd; }));
	if (res == -1 ) { assert(!abort_on_fail); ctx->num_failed++;} else {ctx->num_succeeded++;};
	return 0;
}
